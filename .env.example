# JWT secret key (REQUIRED for production - generate with: python -c "import secrets; print(secrets.token_urlsafe(64))")
JWT_SECRET=

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
